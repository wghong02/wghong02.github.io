import type { Metadata } from "next";

export const metadata: Metadata = {
	title: "Serenest Support",
	description: "Support and contact information for the Serenest retirement planning app.",
};

const SerenestSupportLayout = ({ children }: { children: React.ReactNode }) => {
	return children;
};

export default SerenestSupportLayout;
