{
	"name": "page",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev",
		"build": "next build",
		"start": "next start",
		"lint": "next lint",
		"export": "next export",
		"deploy": "next build && echo > out/.nojekyll && git checkout -B gh-pages && git add -f out && git commit -m 'Deploy' && git push origin gh-pages --force && git checkout main"
	},
	"dependencies": {
		"@headlessui/react": "^2.1.2",
		"@heroicons/react": "^2.1.5",
		"next": "14.2.5",
		"react": "^18",
		"react-dom": "^18"
	},
	"devDependencies": {
		"@types/node": "^20",
		"@types/react": "^18",
		"@types/react-dom": "^18",
		"autoprefixer": "^10.4.20",
		"postcss": "^8.4.41",
		"tailwindcss": "^3.4.10",
		"typescript": "^5"
	}
}
